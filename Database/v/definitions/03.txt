module
	block.always
		block
		endblock
		block.if
		endblock
	endblock
endmodule
ERRMSG
Detected an if block later in an always block.  This will cause a synthesis error due to it looking for if blocks at the top of an always block.